spring:
  datasource:
    url: jdbc:mysql://localhost:3306/carrito_db
    username: root
    password: 123456789

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: always